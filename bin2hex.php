<?php
#bin2hex — Converte um dado binário em representação hexadecimal
$hex = bin2hex('Superheroína');

var_dump($hex);
var_dump(hex2bin($hex));

$hex = bin2hex('Supermilitar');

var_dump($hex);
var_dump(hex2bin($hex));

$hex = bin2hex('Alureano');

var_dump($hex);
var_dump(hex2bin($hex));

$hex = bin2hex('Kryptoniano');

var_dump($hex);
var_dump(hex2bin($hex));

?>